import type { ReactNode } from 'react';

type ReactMarkdownProps = { children?: ReactNode };

const ReactMarkdown = ({ children }: ReactMarkdownProps) => <>{children}</>;

export default ReactMarkdown;